{
  "name": "form-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "ls src/*.js src/*.css src/*.html | entr -sc 'rm build/* && esbuild src/index.js src/index.css --bundle --outdir=build && cp src/index.html build/index.html'",
    "ts:start": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.42",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "@hyperapp/html": "^2.0.0",
    "hyperapp": "^2.0.22"
  }
}
